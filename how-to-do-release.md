
Building
```
npm run build
```
Pushing as usual:
```
git add .
git commit -m "..."
git push
```
Patch
```
npm version patch
```

`npm version` now automatically syncs `manifest.json` version from `package.json`.
